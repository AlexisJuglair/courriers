<form method="post" action="{{ path('home_delete', {'id': courrier.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce courrier ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ courrier.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
